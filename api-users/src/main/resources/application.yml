server:
  port: 8080

spring:
  application:
    name: api-users

  # WebFlux настройки (опционально)
  codec:
    max-in-memory-size: 10MB

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    com.art: DEBUG